<IfModule mod_rewrite.c>
RewriteEngine On

# Se existir arquivo/pasta, serve direto
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Caso contrário, manda para o front controller
RewriteRule ^ index.php [L]
</IfModule>
